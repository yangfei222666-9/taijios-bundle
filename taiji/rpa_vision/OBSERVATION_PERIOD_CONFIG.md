# RPA Vision / Safe Click v0 - 观察期配置

**状态：** ✅ 观察期通过，已封板  
**观察期开始：** 2026-03-11 23:56  
**封板日期：** 2026-04-09

---

## 正式判断

**太极OS 的受控桌面执行能力，已经完成从 0 到 1 的第一阶段闭环。**

这一步最重要的，不是"做出了一个能点的 RPA"，而是**把执行能力做成了有边界、有闸门、有审计的制度化能力**。

这个方向是对的，而且和太极OS现有底座是相容的，不是野生外挂。

---

## 当前状态

**状态：** 已收口，可进入观察期  
**范围：** 只允许 safe_click 小范围使用，不扩主链路权限  
**下一阶段：** safe_type / safe_hotkey 的受控复制，而不是功能发散

---

## 观察期 4 条硬约束

### 1. 只允许走 execute_safe_click() 体系
- 禁止绕过闸门
- 禁止裸函数调用
- 禁止降级执行

### 2. 继续保留审计日志和前后截图
- 每次执行必须写入 `click_audit_log.jsonl`
- 每次执行必须保存 before/after 截图
- 日志和截图不允许删除

### 3. 先做低风险场景复验 3～5 次，确认不是偶然成功
- 不同窗口类型（记事本、浏览器、编辑器）
- 不同文本类型（标题、段落、列表项）
- 不同位置（左中右、上中下）
- 每次复验必须通过 4 项验收标准

### 4. safe_type、safe_hotkey 必须复用同一套闸门和审计模型
- 不另起炉灶
- 不重复造轮子
- 统一审计日志格式
- 统一闸门检查逻辑

---

## 观察期验收标准

### 复验目标（3～5 次）

| 轮次编号 | 窗口类型 | 文本类型 | 位置 | 状态 |
|---------|---------|---------|------|------|
| Round-0-notepad | 记事本 | 内容文本 | 中心偏下 | ✅ PASS (2026-03-11) |
| Round-1-browser | 浏览器 | 段落文本 | 左侧 | ✅ PASS (2026-04-09) |
| Round-2-editor | 编辑器 | 代码注释 | 右侧 | ✅ PASS (2026-04-09) |
| Round-3-filemanager | 文件管理器 | 文件名 | 中心 | 待验证 |
| Round-4-terminal | 终端 | 命令输出 | 上方 | 待验证 |

> 轮次编号用于审计日志、截图、proposal 的交叉对照。所有产出物以编号为前缀归档。

### 验收标准（每次复验必须通过）

1. ✅ 闸门全部放行理由清晰记录
2. ✅ 点击坐标准确落在目标 bbox 内
3. ✅ 前台窗口不切换
4. ✅ 点击后只有低风险 UI 变化
5. ✅ proposal 中的 target_text / bbox / center 与预期点击对象一致（防止"点中但点错"）

### 终端轮特别说明（Round-4-terminal）

- 终端窗口标题会随命令执行动态变化（如 `cmd - python xxx.py`）
- 如果该轮 window_binding 闸门因标题波动而失败，先归因为"标题匹配策略问题"，不归因为 Safe Click 误点
- 需要在报告中明确区分：是标题匹配策略需要调整，还是点击安全本身有问题
- 只有确认标题匹配稳定后，该轮的 PASS/FAIL 才有效

### 封板留档要求

3 轮及以上全部 PASS 后，封板必须附带完整证据包：

- 每轮对应的审计日志片段（从 `click_audit_log.jsonl` 中按轮次编号提取）
- 每轮对应的 before/after 截图（以轮次编号为前缀）
- 每轮的 proposal 记录（target_text / bbox / center / 闸门决策）
- 无证据包的口头 PASS 不算封板

### 观察期通过条件

- 至少完成 3 次复验
- 所有复验全部通过 4 项验收标准
- 无闸门绕过事件
- 无审计日志缺失
- 无高风险区域误点

---

## 观察期禁止事项

### 禁止扩张

- ❌ 不增加新的执行动作（拖拽、滚动、右键等）
- ❌ 不扩大权限范围
- ❌ 不进入主链路
- ❌ 不自动化调用

### 禁止降级

- ❌ 不绕过闸门
- ❌ 不删除审计日志
- ❌ 不禁用截图
- ❌ 不使用裸函数

### 禁止发散

- ❌ 不另起炉灶
- ❌ 不重复造轮子
- ❌ 不引入新的审计格式
- ❌ 不引入新的闸门逻辑

---

## 观察期允许事项

### 允许复验

- ✅ 在不同窗口类型中复验
- ✅ 在不同文本类型中复验
- ✅ 在不同位置中复验
- ✅ 记录复验结果

### 允许优化

- ✅ 优化闸门检查逻辑（不降低安全性）
- ✅ 优化审计日志格式（不破坏兼容性）
- ✅ 优化截图保存策略（不删除历史）
- ✅ 优化错误处理（不隐藏错误）

### 允许准备

- ✅ 设计 safe_type 闸门逻辑
- ✅ 设计 safe_hotkey 闸门逻辑
- ✅ 准备复用代码
- ✅ 准备测试用例

---

## 观察期报告

### 每次复验后输出

1. 目标文本
2. target_bbox
3. click_point
4. 放行日志摘要
5. 点击前后窗口标题
6. 点击前后截图差异结论

### 观察期结束后输出

1. 复验次数统计
2. 通过率统计
3. 闸门拒绝统计
4. 审计日志完整性检查
5. 下一阶段建议

---

## 观察期节奏

**核心原则：**

> 现在这只"手"已经长出来了，下一步不是让它乱抓东西，而是先确认它稳、准、可控、可追责。

**执行节奏：**

1. 先守住当前闭环（7 天观察期）
2. 再做低风险场景复验（3～5 次）
3. 最后才考虑扩展新动作（safe_type / safe_hotkey）

**不追求：**
- ❌ 快速扩功能
- ❌ 大范围应用
- ❌ 自动化调用

**只追求：**
- ✅ 稳定可靠
- ✅ 可控可追责
- ✅ 制度化执行

---

## 观察期闸门

**观察期内任一项失败，当天不进入扩展工作，只允许修破口和补记录。**

这不是建议，是硬约束。

### 5 项检查（每日）

1. ✅ 闸门绕过检测 - 新改动是否绕过 `execute_safe_click()`
2. ✅ 审计日志完整性 - 是否有执行未记录
3. ✅ 截图保存完整性 - 是否有执行未截图
4. ✅ 高风险区域误点检测 - 是否有误点事件
5. ✅ 制度执行检查 - 是否有违反 `CONTROLLED_EXECUTION_SPEC.md` 的行为

---

## 观察期结束条件

### 通过条件（全部满足）

1. ✅ 至少完成 3 次复验
2. ✅ 所有复验全部通过
3. ✅ 无闸门绕过事件
4. ✅ 无审计日志缺失
5. ✅ 无高风险区域误点
6. ✅ 无制度违反事件

### 失败条件（任一触发）

1. ❌ 复验通过率 < 100%
2. ❌ 发生闸门绕过事件
3. ❌ 发生审计日志缺失
4. ❌ 发生高风险区域误点
5. ❌ 发生制度违反事件

---

## 观察期后续

### 通过后

- 进入 v0.1 阶段
- 开始设计 safe_type / safe_hotkey
- 复用当前闸门和审计模型
- 继续小范围验证

### 失败后

- 回滚到观察期前状态
- 分析失败原因
- 修复破口
- 重新进入观察期

---

**配置生效时间：** 2026-03-11 23:56  
**配置维护者：** 小九  
**最后更新：** 2026-03-11 23:56

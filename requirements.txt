# TaijiOS bundle · core runtime deps (v1.1.3)

# 必装 (核心)
requests>=2.28
python-dotenv>=1.0

# API Server (api_server.py)
fastapi>=0.100
uvicorn[standard]>=0.20
pydantic>=2.0

# === 以下为可选 · 按需装 ===
# openai>=1.0            # TaijiOS-Lite / zhuge-skill/core/llm.py 走 OpenAI SDK 时
# anthropic>=0.40        # vision.py 原生 SDK (当前走 urllib 不强依赖)
# edge-tts>=6.0          # aios/voice 备选 TTS
# mcp>=1.0.0             # MCP server 集成
# python-docx>=0.8       # TaijiOS-Lite Office 文档
# websocket-client>=1.5  # Doubao TTS v3 流式
# pyaudio>=0.2.11        # 本机语音输入输出
# pytest>=7.0            # dev/test only
